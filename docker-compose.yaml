services:
  build-rms:
    build:
      context: .
      dockerfile: Dockerfile
    image: aniqamir/daily-dose-docker-img:v3
    container_name: daily-dose-docker-container
    ports:
      - "8000:5050"